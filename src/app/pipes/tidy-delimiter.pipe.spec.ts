import { TidyDelimiterPipe } from './tidy-delimiter.pipe';

describe('TidtDelimiterPipe', () => {
  it('create an instance', () => {
    const pipe = new TidyDelimiterPipe();
    expect(pipe).toBeTruthy();
  });
});
